# TDD 验收契约 · COOLFLY 知识运营中台

> 给 Coding Agent：这份文件定义本次开发的全部 required 终止条件。读取同目录上游产物后持续实现、验证、修复和回归；任何验收行未通过都不得结束。

> **执行门禁（严禁跳过）**：完整 `/goal` 在改任何代码前，必须提取本文件全部验收 ID，创建 `output/tests/TDD验收结果.md` 并全量置为 `pending`。只有发布候选阶段严格按"验证方式"真实执行并留下证据，才能改为 `pass`。未建台账先写代码、候选验收时未真实执行便凭推测倒填、用旧日志标 `pass`，均视为 TDD 没有执行。

> 结果文件元信息固定写成 `- contract_sha256: sha256:<64位hex>` 与 `- run_started_at: <ISO 8601>`，不得改字段名。

> 完成前必须用 tdd-master 校验器的 `--result` 模式或等价脚本机械核对契约与结果；严禁人工目测后宣布全绿。

## 0. 契约元信息

- work_type: `feature`
- source_contract: `requirements.md`
- scope: `MVP`
- source_revision: `revision 5（08-04-2026 D-10 架构转向：active 54 对象 = US×12 + REQ×13（REQ-F09-02/04/07/08/11…19）+ AC×27（AC-F09-06/10/14…38 ↔ PRD AC-P-01…25）+ NFR×2（NFR-002/004）；retired 135 条一律不进本契约）`
- generated_from: `PRD详细版.md（951 行：§0.2 完成定义/§5.10 十视图/§6 三图/§7 异常/§8 指标/§0.5 前置）+ 设计追溯矩阵.md rev5 + 设计决策蓝图.md rev3 + 技术方案.md v2.0 + 页面清单.md rev3 + pages/阶段1_知识运营_知识运营中台.md + pages/知识运营中台v3.dc.html（权威原型，运行时 support.js）`

## 1. 完成定义

同时满足以下条件才算完成（对齐 PRD §0.2：MVP = 知识运营中台十视图五组 + Zendesk 配置迁移 + 飞书 120+ 篇初始迁移消化）：

1. 安装、构建、启动与全部冒烟验收通过。
2. 本文件 §2–§5 的每一条验收行都通过，没有跳过、弱化或删除。
3. requirements.md revision 5 全部 active required Source IDs（13 REQ + 27 AC + 2 NFR）均被验收行覆盖。
4. 三条铁律在真实运行系统中成立并有证据：①统一过审——无人审不生效、任何角色无绕过审核的发布路径、四眼原则；②中台 = 知识唯一权威源、单向同步 Zendesk、drift 检出可处置；③中文 = 内容权威源——英文人工校验后才可同步、内部段落不翻译不外发。
5. MVP 唯一页面 PAGE-F09-01（十视图五组）已落地；仅对 v3 原型 HTML 明确呈现的状态完成同内容区尺寸的原型/成品截图对照，MD/AC 有而原型未呈现的状态以行为证据验收。
6. 最终提供 fresh verification evidence：实际命令、退出码、测试结果和截图对；不能用"应该通过"或"UI 自动化已过"代替视觉验收。

## 2. 冒烟门禁

> 保持 1–3 条。至少覆盖安装/构建/启动和一条核心用户旅程；同一条可包含多个连续动作。

| ID | Source IDs | Design IDs / 产物 | 场景与操作 | 可观察通过标准 | 验证方式 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `SMOKE-01` | - | 技术方案.md §2/§4（自研中台单体 + 数据库迁移与种子数据） | 从干净环境执行依赖安装、前后端构建、数据库迁移/种子初始化、服务启动 | 安装与构建命令退出码 0；服务进程启动成功、健康入口可访问；启动日志无阻断性运行时错误 | 真实命令与退出码（安装/构建/迁移/启动） |
| `SMOKE-02` | REQ-F09-19、REQ-F09-11 | PAGE-F09-01、API-F09-07、CMP-F09-11 | 以种子账号（知识管理员）经登录页登录，进入中台首屏「我的工作台」，随后逐一点击五组导航下的十个视图 | 登录成功后落在我的工作台，四统计卡按当前账号渲染计数；十视图全部可达、面包屑随导航联动；界面为简体中文；浏览器控制台与服务端均无阻断错误 | 浏览器 E2E（登录 + 十视图导航走查） |

## 3. 功能与流程验收

> 按用户可观察结果和核心旅程组织，不按函数、组件、页面或测试层拆分。一条场景可覆盖多个 Source/Design ID。

| ID | Source IDs | Design IDs / 产物 | 场景与操作 | 可观察通过标准 | 验证方式 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `FLOW-01` | REQ-F09-02、REQ-F09-11、REQ-F09-15、REQ-F09-16、AC-F09-14、AC-F09-23 | SEQ-F09-01、API-F09-08、API-F09-09、API-F09-10、CMP-F09-23、CMP-F09-26 | 知识管理员手工录入一条真实中文条目（如「无人机首次配对失败排查」）提交审核；回工作台核对计数；换知识审核员账号在审核中心查看该条（来源徽章「人工录入」）与变更前后对照后通过；门禁四查全过后观察同步中心（Zendesk 端点联调或契约 Mock） | 状态按 草稿→待审核→审核通过→已发布 流转；提交后条目仅出现在审核中心、提交人工作台「我提交待审」+1、待审计数在导航实时可见；过审前条目不在知识库总览且不产生同步任务；过审+门禁通过后自动生成同步任务、四统计卡计数与任务列表一致、条目出现在知识库总览对应章节 | 浏览器 E2E 全旅程 + 集成断言（同步队列唯一写入源 = 发布 API 成功路径） |
| `FLOW-02` | AC-F09-24、REQ-F09-15、REQ-F09-11 | CMP-F09-24、API-F09-09、CMP-F09-11 | 审核员驳回一条待审条目：先空理由点确认，再填写真实理由提交；提交人回「我的工作台」→「被驳回待改」，修改后重新提交 | 空理由不可提交且必填校验提示可见；驳回后条目回提交人草稿箱、状态「已驳回」、理由回传并在提交人工作台可见；重新提交后审核历史时间线含完整往返记录（驳回理由 + 重提） | 浏览器 E2E + 集成断言（审核历史留痕） |
| `FLOW-03` | REQ-F09-14、AC-F09-21、AC-F09-22 | CMP-F09-21、CMP-F09-22、API-F09-11、API-F09-16、DATA-F09-12 | 构造近四日挖掘批次数据（含一个成功、一个空批次、一个失败批次）打开视图④；对一条查重 0.88 的候选、一条新增类候选、一条丢弃候选分别处置 | 批次行展示 来源会话数分渠道（邮件工单/在线聊天）、候选数、状态；空批次标「无新候选（未达频次阈值）」、失败批次标原因（如 Zendesk API 429 限流）且次日照常拉取，无静默缺批次；候选卡三重准入字段（频次/查重值/缺口判定）与类型徽章齐全；查重 ≥0.85 不提供「立新条」仅可「挂为修订建议」；起草处置产物只出现在审核中心（来源「AI 挖掘」）、无直接入库路径；丢弃留痕且同主题再达阈值可重现 | 集成测试（批次三态与三重准入构造）+ 浏览器 E2E（候选处置） |
| `FLOW-04` | AC-F09-19、REQ-F09-13 | CMP-F09-19、API-F09-09、DATA-F09-09、SEQ-F09-02 | 对一条已发布条目完成一次修订并过审生成 v2；打开版本页签查看列表与 v1/v2 对比；以审核员执行回滚到 v1（经确认弹窗），再以知识管理员寻找回滚入口 | 版本列表含各版效果（调用/命中/解决/采纳）；任选两版左右并排 diff（删除行红底、新增行绿底）；确认回滚后 v1 重新生效、v2 标「已回滚」、历史版本与指标保留、动作写审计日志并自动生成同步任务；知识管理员无可用回滚入口（禁用并说明） | 集成断言（回滚事务 + 自动入同步队列）+ 浏览器 E2E |
| `FLOW-05` | REQ-F09-17、REQ-F09-07、AC-F09-30、AC-F09-31、AC-F09-32、AC-F09-33 | CMP-F09-29、CMP-F09-30、API-F09-12、API-F09-13、DATA-F09-13 | 构造 Zendesk 信号数据（含待核实档信号、低解决率条目、引用数低于样本下限条目、搜索无结果关键词）；打开视图⑧把一条修订候选转为建议；再打开视图⑦逐一核对三页签 | 信号矩阵四渠道行各含命中信号、解决信号、确定性档位（必得/档位相关/待核实如实区分）；五来源修订候选处置后进审核队列（来源「反馈修订」）并留痕；知识库效果页签场景覆盖条低覆盖标红、低解决率条目标红加粗浮顶、样本不足条目显示「样本积累中」不显示数值、点行直达条目效果页签；知识缺口页签缺口→动作列表与搜索无结果关键词列表可见、动作按钮一键起草或挂修订进审核队列；客服工作数据页签无重建的工单量/首响/CSAT 图表，仅 Explore 口径说明与指向 + 「去知识缺口」直达 | 集成测试（信号档位与聚合三态构造）+ 浏览器 E2E |
| `FLOW-06` | REQ-F09-13、AC-F09-29、AC-F09-17、AC-F09-25 | CMP-F09-17、DATA-F09-10、API-F09-08、API-F09-09、SEQ-F09-02 | 中文条目触发 AI 翻译→逐段人工校验（含一段人工修订）→标记「已确认」；随后修改中文正文保存；另构造一条英文状态「待人工校验」的条目走完审核通过 | 英文状态机 未生成→AI 翻译中→待人工校验→已确认→已同步 逐态可见且为服务端强制；「已确认」前「同步到 Zendesk」禁用；中文保存后英文自动置「待重新校验」、同步被阻断且原因可见、向量状态置「待重建」；人工修订段落带修订标注；英文未确认条目过审后门禁四查展示（格式字段/内部段落标记/英文状态/代理评测）、不入同步队列、阻断原因可见、代理评测附「代理指标不等于 Zendesk AI 真实表现」标注 | 集成断言（保存联动与状态机守卫）+ 浏览器 E2E |
| `FLOW-07` | REQ-F09-12、AC-F09-15、AC-F09-16、AC-F09-20 | CMP-F09-12、CMP-F09-13、CMP-F09-14、API-F09-08、DATA-F09-08 | 打开知识库总览切换三个知识库并折叠展开结构树；执行搜索与组合筛选（场景/状态/可见性/复核到期）；构造复核超期与临期条目各一；尝试删除一个含条目的章节 | 三库可切换且「客服话术库·仅内部」带不对外说明；结构树 目录→章节→条目 两级可折叠、章节行带 Zendesk Section 映射标识、结构映射说明常驻；条目行含 状态/可见性/版本/英文状态/同步状态/解决率/复核到期/向量化状态；筛选即时过滤且结果标注归属路径；超期条目标红（含超期天数）并被「已到期」命中、临期标黄；含条目章节删除被拦截并提示先移空，结构维护变更即时反映并留痕 | 集成测试（筛选、复核时间窗与删除拦截构造）+ 浏览器 E2E |

## 4. 设计落地验收

> 页面清单中每个 MVP 页面恰好对应一行，不合并多个 PAGE ID。页面、弹窗和关键状态都要实现；视觉对照只覆盖 HTML 明确呈现的状态，HTML 未呈现的状态按 MD/AC 使用行为证据验收，不生成或脑补视觉基线。验证真实运行页面，不验证静态 HTML 文件存在。通过标准必须从原型提取具体锚点，禁止只写"与原型一致"。

> ⚠️ **产物路径警告注记（规范容错条款）**：PAGE-F09-01 的页面 MD 与原型 HTML 主文件名不一致——MD = `pages/阶段1_知识运营_知识运营中台.md`，HTML = `pages/知识运营中台v3.dc.html`（用户自制 v3 权威原型收编件，运行时依赖同目录 `support.js`，双文件须一起部署）。命名偏差已在设计追溯矩阵 §0 与 DEC-014 登记，按实际路径验收，不阻塞。旧五区版产物在 `pages/archive-v2/` 已标注归档（page-id-archived），不得作为验收基线。

| ID | Source IDs | Design IDs / 产物 | 场景与操作 | 可观察通过标准 | 验证方式 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `DESIGN-01` | REQ-F09-08、AC-F09-10 | PAGE-F09-01；pages/阶段1_知识运营_知识运营中台.md；pages/知识运营中台v3.dc.html（权威原型，运行时依赖同目录 support.js） | 以 http 服务运行 v3 原型（交互式 React 运行时）与成品，在同一 1280+ 内容区尺寸、相同演示数据与交互状态下逐视图对照十视图，含左下角四角色切换后的禁用/只读态与原型呈现的 已驳回/待人工校验/翻译失败/已阻断/无新候选 等演示状态；MD/AC 要求而原型未呈现的状态（登录与首次改密页、驳回空理由校验、回滚确认弹窗、409 并发冲突提示等）不做视觉对照、以 FLOW/RULE 行为证据验收 | 结构=左侧 248px 固定侧栏（COOLFLY 标识＋副标「运行时 = Zendesk · 本台 = 唯一权威源」）＋五组分组导航（工作台／知识生产／质量与发布／数据／治理）承载十视图＋面包屑与主内容区、右上当前身份与左下角色切换常驻；组件=导航徽标计数（如知识库总览 96、AI 对话挖掘 6、同步中心 3、反馈回流 12）、工作台四统计卡与分组列表、总览三库切换卡＋两级结构树＋条目列表、条目工作台状态流转条＋四页签（正文与字段侧栏／版本 diff／效果／条目日志）＋向量状态面板、挖掘批次卡与候选卡、审核四来源队列＋门禁四查清单、同步四统计卡＋任务列表＋drift 报警区＋结构映射表、看板三页签、信号矩阵表、日志时间线四页签过滤、RBAC 三页签与权限矩阵表；内容=全部界面元素与示例数据 100% 简体中文（品牌词 COOLFLY 除外）、演示数据含 已驳回／待人工校验／待重新校验／翻译失败／已阻断／同步失败／无新候选／样本积累中 等状态字样且与 PRD §5.10 文案定稿口径一致；交互=十视图导航切换与面包屑联动、四角色切换后按钮禁用与只读态即时变化（知识管理员「发布并同步」恒禁用并提示铁律、AI 运营编辑器只读、非审核员通过按钮禁用）、点条目行进条目工作台、点批次展开候选、点统计卡切换分组；视觉=浅青蓝底（#EAF6F8）白卡侧栏与主色 #0D7594 的层级、导航当前项高亮、失败与阻断行红标置顶、复核超期红临期黄、diff 删除红底新增绿底；原生适配=无（桌面 Web 1280+ 单形态） | 运行原型与运行成品同视图同内容区尺寸 PNG 截图对（十视图与角色切换态按相同顺序拼图，存 tests/visual/）+ 独立视觉验收官实际查看两图逐项核对结构/组件/内容/交互入口/视觉层级；原型未呈现状态用真实页面操作或浏览器 E2E 行为证据；UI 自动化不能代替已有视觉基线的视觉验收 |

## 5. 风险触发验收

> 没有真实高风险就写"本期无额外风险验收"，不要为凑类别造用例。存在金额、权限、隐私、重要数据、不可逆操作、外部契约或明确 NFR 时，每个独立风险至少一行。

| ID | Source IDs | Design IDs / 产物 | 场景与操作 | 可观察通过标准 | 验证方式 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `RULE-01` | REQ-F09-19、AC-F09-35 | CMP-F09-32、API-F09-14、API-F09-07、DATA-F09-14 | 分别以四角色登录逐项走查越权动作：AI 运营尝试改正文/审核/发布/回滚/同步重试，知识管理员尝试审核通过/发布并同步/回滚/改权限矩阵，知识审核员尝试修改权限矩阵/创建用户，系统管理员尝试审核与发布知识内容；再以非系统管理员会话直接调用 /rbac 权限矩阵接口 | 全部越权动作被界面禁用并说明原因或明确拒绝，与 PRD §5.10 权限矩阵 10 项×4 角色逐项一致；接口层同样拒绝（不能仅前端禁用）；权限矩阵仅系统管理员可修改且每次修改写审计日志、修改即时生效；系统管理员不参与知识内容审核与发布（职责分离） | 集成测试（矩阵逐格断言 + 接口越权请求）+ 浏览器 E2E 四角色走查 |
| `RULE-02` | REQ-F09-04、AC-F09-18、AC-F09-36 | API-F09-09、CMP-F09-15、SEQ-F09-01、DEC-019 | ①知识管理员对任一条目寻找发布路径（界面遍历 + 直接调用发布接口）；②知识审核员提交自己的条目后尝试自审通过；③构造未过审条目并检查其对外可见面 | 「发布并同步」对知识管理员恒禁用并提示统一过审铁律文案、直接调用发布接口被拒；提交人=当前审核员时通过按钮禁用并说明四眼原则、approve 接口拒绝、须由另一审核员通过；未过审条目不产生同步任务、不出现在知识库总览与（Mock/联调）Zendesk 端——同步队列唯一写入源=发布 API 成功路径的构造性断言通过 | 集成测试（状态机守卫 + 写入路径断言 + 四眼接口断言）+ E2E 界面走查 |
| `RULE-03` | REQ-F09-02、AC-F09-06、AC-F09-38 | API-F09-08、API-F09-03、CMP-F09-23、DATA-F09-08 | 构造含格式错误与字段不全行的 Markdown/CSV 脏文件执行批量导入（模拟飞书 120+ 篇一次性迁移批），随后对至少一条导入条目走完过审与同步 | 成功条目全部进审核中心待审队列（来源徽章「批量导入」），不存在直接入库；失败条目逐条报告行号与原因、不整批静默失败、不静默丢行；未过审导入条目不同步；过审条目按可见性同步至 Zendesk 对应 Section 或客服 segment——迁移全程无绕审入库路径 | 集成测试（脏文件导入构造）+ E2E 抽查 + 初始迁移演练记录 |
| `RULE-04` | AC-F09-27、REQ-F09-16 | API-F09-16、CMP-F09-16、DATA-F09-11、DEC-015 | 构造三种可见性条目（对外公开／仅客服内部／混合含内部段落）各走完过审并同步一次，检查同步载荷与目标端 | 对外公开→帮助中心文章；仅客服内部→仅客服 segment、对外帮助中心不可见；混合条目内部段落经服务端剥离后不出现在对外文章正文与英文翻译中（对同步载荷做内容断言——内部口径外泄属数据泄漏级失败，零容忍） | 集成/契约测试（同步载荷内容断言，服务端剥离 + 门禁第②查双保险）+ 联调抽查 |
| `RULE-05` | NFR-002、AC-F09-37、REQ-F09-19 | API-F09-07、API-F09-14、DATA-F09-12、DATA-F09-14、DEC-017 | ①系统管理员创建用户（选角色与知识库范围）→新用户首次登录→随后禁用该用户并测其在途会话；②向挖掘管道输入含邮箱/SN/家庭 Wi-Fi 名称的样例会话；③核验 LLM 供应商 DPA 与「输入不用于训练」条款证据留档 | 创建用户必选角色与知识库范围（可授单库）、新用户首次登录被强制改密；禁用后该用户在途会话即时失效、历史操作留痕保留、可再启用；创建与禁用均写审计日志；落库候选的来源摘要中邮箱/SN/Wi-Fi 名称已打码（脱敏纯函数测试通过、原文不落库）；DPA 证据留档——任一项未就绪如实记录为不满足上线条件 | 集成测试（会话即时失效断言 + 脱敏单测）+ 浏览器 E2E + 上线前检查单证据 |
| `RULE-06` | REQ-F09-16、AC-F09-26、AC-F09-28、NFR-004 | CMP-F09-26、CMP-F09-27、CMP-F09-28、API-F09-10、API-F09-16、DATA-F09-11、SEQ-F09-03、DEC-018 | ①构造同步失败（Zendesk 接口 5xx/429）与英文未确认阻断各一条；②在 Mock/沙箱 Zendesk 端直接改写一篇已同步文章触发 drift 比对，分别执行「以本台覆盖」与「拉回进审核队列」两种处置；③两个账号并发编辑同一条目后先后提交；④构造目标 Section 不存在的结构映射 | 失败行自动重试（建议值 3 次）后告警、置顶标红、手动重试仅审核员可点、失败不影响 Zendesk 端上一版继续服务；阻断行展示原因、五档状态计数与列表一致、不存在「阻断但已同步」中间态；drift 报警含 文章/修改方/差异摘要/时间，两种处置均生效且写审计日志（拉回内容成为审核中心待审来源）；后提交者收冲突提示、不静默覆盖、修订历史可回溯；映射目标不存在时同步失败并提示先修复映射、不自动创建未经确认的 Zendesk 结构 | 集成测试（同步状态机 + drift 构造 + 乐观锁冲突 + 映射失败构造）+ 浏览器 E2E |
| `RULE-07` | REQ-F09-18、AC-F09-34、NFR-004 | CMP-F09-31、API-F09-15、DATA-F09-14 | 连续完成 提交→驳回→重提→通过→发布→回滚→权限修改 全链路后打开视图⑨；再在应用与数据层尝试对既有日志行执行修改与删除 | 每一步均有记录且含 时间/操作人（真实账号+当时角色）/对象/动作徽章/字段级前后值（如 退款时限：7 天→5 天、发布权限：允许→禁止）/备注（含驳回理由）；四页签过滤（内容变更/审核与发布/权限与系统）生效；日志只增不改不删（无 UPDATE/DELETE 路径、改删尝试被拒）、长期保留；可见范围按角色生效（审核员与系统管理员全量、其余与己相关） | 集成测试（全链路留痕断言 + append-only 断言）+ E2E 抽查 |

## 6. 本期不阻塞项

> 只写明确延后的质量项及理由，不生成测试 ID，不计入 GOAL。

- 性能与并发压力测试——无性能 NFR（NFR-003 已 retired），内部工具十人级并发，MVP 延后。
- 完整无障碍审计——NFR-001 已 retired，中台桌面 Web 无无障碍需求登记，后置 App 接入阶段重启。
- 浏览器兼容矩阵——内部系统按桌面主力浏览器（1280+）验证即可，多浏览器矩阵延后。
- 中台 UI 多语言切换——PRD §0.1 阶段二 planned（PAGE-F07-01 登记），MVP 仅保证文案走语言资源不硬编码。
- Zendesk 生产环境全量联调与核实清单①③⑤（订阅档位/automated resolution 计费口径/Guide 编辑权限收权）——owner=用户团队，核实前相关信号按「待核实」档如实标注、不进达标判定；本契约以 Mock/沙箱联调为验收态。
- 「建议值 · 终稿前确认」类数值（重试次数、样本下限、标红线、复核周期、drift 频率等）——按当前建议值实现并可配置，终稿数值确认不阻塞本轮验收。
- 方案 PPT 重生成——PRD 链路配套汇报产物，非本契约验收对象。
- App 端任何改动——D-10 拍板本期完全不动（PAGE-F08-02 后置登记）。
