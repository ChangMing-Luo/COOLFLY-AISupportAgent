# Claude Code local (per-machine) settings — not shared
.claude/settings.local.json
app/node_modules/
app/*/node_modules/
app/*/dist/
app/.sandbox-zendesk.json
app/server/.sandbox-zendesk.json

# 本地数据库备份（迁移前快照，含业务数据，不入库）
.backup/
